info:
  name: List Sessions
  type: http
  seq: 4
  tags:
    - sessions

http:
  method: GET
  url: "{{baseUrl}}/sessions/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/sessions/list"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""

docs: List all sessions.
